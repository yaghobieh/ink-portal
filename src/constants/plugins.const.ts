export type InkPluginCatalogEntry = {
  id: string;
  name: string;
  packageName: string;
  npmUrl: string;
  gitUrl: string;
  description: string;
};

export const PUBLIC_PLUGINS_PATH = '/api/public/plugins';
